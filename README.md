# Карта населения Московской области

Интерактивная векторная карта для просмотра населения и плотности населения по муниципалитетам Московской области.

## Что можно посмотреть

- 56 муниципальных образований на векторной карте;
- переключение между населением и плотностью населения (чел./км²);
- карточку выбранного округа с площадью, населением и плотностью;
- 10 крупнейших городов: их население и плотность соответствующего округа.

## Запуск

Требуется Node.js 22.13 или новее.

```bash
npm install
npm run dev
```

После запуска откройте адрес, который появится в терминале (обычно `http://localhost:3000`).

## Данные

Границы муниципалитетов и распределение населения подготовлены как GeoJSON-подобные векторные данные. Витрина использует муниципальные итоги Мосстата на 01.01.2025; пространственная детализация основана на GHS-POP 2020. Файл данных находится в `public/data/moscow-oblast-municipalities.json`.

Каждый объект содержит идентификатор и название территории, геометрию, площадь в км², население и рассчитанную плотность. Поэтому данные можно использовать не только для отображения, но и в аналитике или моделях.

## Проверка сборки

```bash
npm run lint
npm run build
```

В папке `dist/` уже лежит собранный артефакт этой версии. Для повторного запуска всё равно потребуются зависимости из `npm install`.
